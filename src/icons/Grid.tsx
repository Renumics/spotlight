import type { IconType } from 'react-icons';
import { RiLayoutGridLine } from 'react-icons/ri';
import tw from 'twin.macro';

const Grid: IconType = tw(
    RiLayoutGridLine
)`w-4 h-4 font-semibold inline-block align-middle stroke-current`;
export default Grid;
