import type { IconType } from 'react-icons';
import { BiDirections } from 'react-icons/bi';
import tw from 'twin.macro';

const Tour: IconType = tw(
    BiDirections
)`w-4 h-4 font-bold inline-block align-middle stroke-current`;

export default Tour;
